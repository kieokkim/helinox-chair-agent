# 헬리녹스 매장 호환 규칙집

대화 8턴에 걸쳐 도출된 규칙. 새로 쓰지 않고 대화에 있던 판단을 그대로 옮김.
에이전트 프롬프트의 원재료로 사용할 것.

## 범위

아웃도어 라인 체어 30개, 코트 7개, 테이블 8개(2026-08-12~18 기준,
helinox.co.kr). 택티컬 12개·HDB 7개·텐트·트레킹폴·소프트굿즈는 이
규칙집 범위 밖. 아래 "스펙그룹 판정 규칙"부터 "예외표"까지는 체어
기준으로 작성됐고, 코트·테이블 관련 규칙은 뒤쪽 "코트·테이블 확장"
섹션에 따로 있음 — 서로 다른 카테고리라 판정 축이 다름.

## 스펙그룹 판정 규칙 (1줄)

원단·프레임·가격 중 하나라도 다르면 새 스펙그룹. 색만 다르면 같은 스펙그룹.

## 성격블록 판정 순서 (5줄, 위에서부터 처음 걸리는 곳에서 멈춤)

1. 스툴 구조인가? → **스툴** (스피드스툴 S/M, 벤치원)
2. 경량 계열인가? → **경량** (체어제로 전 모델)
3. 목받침 있나? → **하이백**
4. 목받침 없나? → **로우백**
5. 그 외 → **기타**

주의: "스툴" 명칭은 실제로는 "액세서리 미대응"에 가까움(벤치원은 스툴이 아니라
내하중 190kg의 대형 벤치). 이름과 실제 성격이 다르다는 걸 신입에게 짚어줄 것.

## 라인 존재 규칙 (3줄, 원 26모델 기준 100% 검증됨)

- 아웃도어는 전 모델 존재 (26/26 = 100%)
- 경량 계열은 아웃도어 전용 — 택티컬·HDB 없음 (4/4 = 100%)
- HDB는 로우백·하이백에만 존재 — 스툴·기타·경량 없음 (7/7 = 100%)

## 변형 제약 (1줄, 원 26모델 기준 100% 검증됨)

(re)·LT·메쉬 표기가 붙은 변형은 아웃도어 라인에만 존재. 택티컬·HDB에는 표기 없음
(존재 자체가 없거나, 있어도 표기를 안 함 — 후자는 미검증).

## 변형 2분류 (1줄, 핵심 규칙)

- **스킨 변형** (LT·메쉬): 프레임 그대로, 5축 전부 상속. 신입에게 외우게 하지 말 것.
- **구조 변형** (하이백·라지·미니·XL): 새 플랫폼. 축마다 개별 확인 필요.

## LT 판정 규칙 (1줄)

같은 이름의 비LT 제품(부모)이 있으면 → 스펙그룹, 5축 상속.
부모가 없으면 → 독립 플랫폼 (예: 테이블제로 LT — 이 프로젝트 범위 밖 제품이나
"LT=항상 부모 있음" 규칙의 유일한 예외로 기록).

## 세대(구형/신형/re) 표기 규칙 (2줄)

- (re) 표기 = 메이저 개정. 호환 다수 변경(체어원: 볼핏·그라운드시트·락킹풋 3축).
- 무표기 세대차(구형→신형) = 마이너 개정. 퍼스널쉐이드 1축만 영향, 나머지 4축 동일.
- 세대는 별도 플랫폼 아님 — 쉐이드 축 1개만 세대에 따라 갈림.

## 패턴 규칙 (2줄, 예외 있음 — traps.csv 참고)

- 하이백 블록은 대체로 퍼스널쉐이드 O.
- 경량·스툴 블록은 락킹풋·퍼스널쉐이드 항상 X.

## 액세서리 관리 원칙 (2줄)

- 액세서리는 위치·호환(종속여부)만 관리. 자체 스펙은 보유하지 않음(무게만 예외로 게재).
- 액세서리 호환은 결정축(7종: 다리끝규격/폴직경/풋프린트제품군/제품군/모델별/
  디자인라인/코트등급/테이블사이즈) 중 하나로 계산됨. 기어×액세서리 전체 매트릭스로
  관리하지 않음 — 결정축이 있으면 N×M이 필요 없음.

## 라인이 호환을 가르는 유일한 사례 (1줄)

사이드스토리지(택티컬 웨빙 전용). 나머지 5축(볼핏·그라운드시트·락킹풋·쉐이드·컵홀더)은
전부 라인 무관, 프레임이 결정.

---

## 이름 함정 (암기 항목, 12건)

전체 목록은 `traps.csv` 참조. 온보딩 1일차에 이 12건만 먼저 짚어주는 것이
130칸 표 전체를 외우게 하는 것보다 효율적임 (5.9배 압축).

---

## 예외표 (원 26모델 기준 3행, 1.2% — 30모델 확장 후 재계산 필요)

| # | 내용 | 상태 |
|---|---|---|
| 1 | 코트레그→볼핏45mm: 사용가능(비권장), 2단 체인 | 유지 |
| 2 | 사이드스토리지: 라인(택티컬)이 결정하는 유일한 액세서리 | 유지 |
| 3 | 체어원 미니 컵홀더: 로우백인데 유일하게 불가 | 유지 |

30모델 기준 조합 = 30 × 5(축 종류로 압축, 매트릭스 아님) — N×M 매트릭스를
쓰지 않으므로 "예외표÷전체조합" 비율 재계산은 accessories.csv 완성 후 진행.

---

## 열린 이슈 (에이전트 구현 전 확인 필요)

1. ~~그라운드시트 명칭 불일치~~ — **해결됨 (사용자 재검증, 2026-08-18).**
   공식몰은 인치 사이즈가 아니라 One/Two/XL/Sunset/Zero/Swivel 등 의자별
   명칭으로 그대로 판매함. rules.md v1 작성 시 이 항목은 실물 확인 없이
   적어 넣은 미검증 우려였음 — 실제로는 문제가 아니었음. 에이전트 응답
   로직에서 이 우려를 반영한 부분이 있다면 제거할 것.

2. **성격블록 명칭 "스툴"**: 실제로는 "액세서리 미대응"에 가까운 그룹. 벤치원이
   스툴처럼 안 생겨서 신입이 헷갈릴 수 있음. chairs.csv 스키마는 "스툴" 유지했으나
   에이전트 응답 문구에서는 "액세서리 미대응 그룹"으로 풀어서 설명 권장.

3. **twins.csv의 LT2·LT3**: 부모-자식 축 값이 실측이 아니라 상속 규칙 추정.
   에이전트가 이 두 그룹 관련 질문에 답할 때 "동일 프레임 규칙으로 추정"이라는
   표시를 붙이도록 twins.csv의 note 컬럼을 프롬프트에 반드시 포함할 것.

4. **플라야 체어**: 5축 전부 [미확인]. 단종+실측 없음. 질문 들어오면
   "데이터 없음"으로 답해야 함(eval Q19/Q20과 같은 유형이나 traps.csv에는
   미포함 — 별도로 인지할 것).

5. **성격블록↔라인 존재 규칙의 예외 가능성**: 이 규칙은 26개 모델에서 100%였으나
   30개로 늘어난 뒤 재검증 안 됨(체어원LT·선셋체어LT·비치메쉬 계열 4개 추가분).
   신규 4개도 전부 아웃도어이므로 규칙 자체는 안 깨졌으나, 택티컬/HDB 수집 시
   제일 먼저 이 규칙부터 다시 확인할 것.

---

## 코트·테이블 확장 (2026-08-18)

### 신규 파일 2개 확보됨 — cots.csv(7행), tables.csv(8행)

지난 라운드 감사에서 발견한 고아 행 7개 중 3개(코트레그12pcs/16pcs, 코트텐트)가
이번에 cots.csv로 해결됐다. accessories.csv의 해당 3행을 갱신했고, 특히
**코트레그 16pcs는 "추정"에서 "공식 확인"으로 격상**됐다 — 이전엔 코트맥스가
16pcs를 쓸 거라고 짐작만 했는데 이번에 실측으로 확인됨.

twins.csv의 `LT4`(코트원↔코트원LT)도 채워졌다. 그것도 그냥 채운 게 아니라
**팩킹 사이즈가 지문처럼 일치**해서(신형케이스 15×53×14, LT도 15×53×14,
구형케이스는 16×53×16으로 다름) 부모가 신형케이스라는 것까지 확정됐다.
이걸로 LT 관계의 실측 앵커가 1개(체어제로LT)에서 2개(+코트원LT)로 늘었다 —
LT 상속 규칙 자체의 신뢰도가 올라간 셈이다. LT2·LT3(체어원LT·선셋체어LT)는
여전히 "추정" 상태 그대로다.

`STANDALONE`(테이블제로 LT, 부모 없는 유일한 LT)도 실측 스펙이 채워졌다.
tables.csv 8개 안에 짝이 되는 "테이블제로"(비LT)가 없다는 것도 재확인됐다 —
"LT는 항상 부모가 있다"는 규칙의 유일한 예외라는 결론이 그대로 유지된다.

### 코트 영문 명칭 정정

코트의 영문명은 COURT가 아니라 **COT**다(2026-08-18, 사용자 확인). 이전에
`courts.csv`와 `COURT1-OUT-*` 형태로 만들어뒀던 걸 전부 `cots.csv`,
`COT1-OUT-*`로 고쳤다. 새로 뭔가를 참조할 때 COURT를 쓰면 존재하지 않는
파일을 찾게 되니 주의할 것.

### 새로 발견된 패턴 — 코트·테이블 (체어에서 못 봤던 것들)

- **하드탑 = LT의 반대 방향.** 체어의 LT/메쉬는 가볍고 저렴한 스킨 변형인데,
  테이블원 하드탑은 테이블원(기본) 대비 무게 +26.4%, 가격 +7.3%로 **더
  무겁고 비싼 프리미엄 스킨 변형**이다. "변형 접미사가 항상 가벼운 쪽을
  뜻하지 않는다"는 걸 이번에 처음 확인했다.
- **코트의 신형/구형 케이스는 호환에 영향 없음.** 체어원 구형→신형→re는
  갈수록 액세서리 축이 바뀌었는데, 코트원·코트맥스의 신형/구형 케이스는
  팩킹 치수만 살짝 다르고 코트레그·코트텐트 호환은 완전히 동일하다. 세대
  표기가 있다고 무조건 호환이 바뀌는 게 아니라는 반례.
- **라이트 코트는 코트레그 호환 불가가 공식 확인됨.** 이전 대화에서
  사용자가 짐작으로 말했던 것이 이번에 공식 페이지 문구로 재확인됐다.
- **사이드테이블은 내하중 단위 자체가 다른 카테고리다.** 스몰 2kg, 미디움
  3kg — 체어 최저(90kg)의 45분의 1이다. 사람이 앉는 물건이 아니라 음료·
  소품 거치대이므로, 나중에 "내하중" 필터를 여러 카테고리에 걸쳐 통합할
  때 이 카테고리는 단위 스케일이 다르다는 걸 감안해야 한다.
- **헤드레스트는 개별 모델이 아니라 성격블록 단위로 호환된다.** 지금까지
  결정축은 물리 규격(다리끝규격·폴직경 등)이거나 개별 모델 나열(모델별)
  이었는데, 헤드레스트는 처음 나온 **"성격블록 그대로가 결정축"인 사례**다
  — 하이백 블록 11개 전부 호환. "체어제로 하이백은 제외"라는 조건도 사실
  아무 효과가 없다 — 체어제로 하이백은 T01 규칙상 애초에 하이백 블록이
  아니라 경량 블록이라서, 말할 필요도 없이 이미 빠져 있었다. 결정축
  종류가 이걸로 8종에서 9종(+성격블록)이 됐다.

### 남은 고아 행 (accessories.csv 26행 중 2행)

```
디자인라인(사이드스토리지) → 택티컬 데이터 필요 (12개, 미수집)
테이블사이즈(테이블탑)      → HDB 데이터 필요 (실존 확인, 미수집)
```

테이블탑은 "존재 자체가 불확실"에서 "실존 확인, 데이터만 없음"으로
바뀌었다(2026-08-18) — 아웃도어가 아니라 HDB(홈데코) 라인 소속 액세서리라
이 프로젝트 범위 밖이었을 뿐이다. tables.csv의 "테이블원 하드탑"과는 다른
제품이니 혼동하지 말 것(하드탑=완제품 변형, 테이블탑=교체형 상판).
헤드레스트 2종은 이번에 해결돼서 목록에서 빠졌다. 4행 → 2행.



---

## v2 변경사항 (2026-08-18) — 클로드코드 구현 지침

v1 eval 19/20 실행 중 발견된 문제 2건을 데이터에서 고쳤고, 남은 1건은
데이터가 아니라 코드(agent_core.py) 쪽 설계를 바꿔야 해서 여기 상세히 설명함.
아래 세 가지를 순서대로 적용할 것.

### 1) chairs.csv에 height_cm 컬럼이 새로 생겼다

`width_cm`, `length_cm` 다음, `pack_size` 앞에 `height_cm`이 추가됐다.
"전개했을 때 전체 높이"를 뜻하고, 지금까지는 아예 없던 컬럼이다. 30개 중 24개는
실측값이 채워져 있고, 단종 6개(체어원구형·비치체어·비치체어메쉬·플라야·캠프·
스피드스툴S)는 다른 스펙 컬럼과 마찬가지로 `[미확인]`이다.

agent_core.py의 CSV 로더가 컬럼을 이름으로 읽는 구조라면(딕셔너리 기반) 코드
수정이 필요 없다. 혹시 컬럼 순서를 인덱스로 하드코딩한 부분이 있다면
(예: `row[13]`처럼) 그 부분만 확인해서 깨지지 않게 고칠 것.

### 2) T09 / Q09가 원래 틀린 주장이었다 — 이번에 정정됨

원래 트랩은 "체어원 엑스라지 89cm가 하이백 최고 모델(81cm)보다 높다"였는데,
81cm는 사실 체어제로 하이백의 값이고 체어제로 하이백은 T01 규칙에 따라
하이백이 아니라 경량 블록 소속이다. 즉 서로 다른 트랩끼리 모순되는 주장을
하고 있었다. 진짜 하이백 블록(11개) 안에서 다시 비교하면:

```
하이백 최저: 체어투 84cm (비치체어 계열 78cm는 라운저라 형태가 달라 제외)
하이백 최고: 사바나 110cm
체어원 엑스라지(로우백): 89cm
```

89 > 84 는 성립하고, 89 > 110 은 성립하지 않는다. 그래서 T09/Q09 정답
문구를 "체어투(84cm)보다 높다. 다만 사바나(110cm)보다는 낮다"로 바꿨다.
이 정정은 `traps.csv`와 `eval_questions.csv`에 이미 반영돼 있으니, `main.py`
안에 EVAL_CRITERIA를 이 문항 문구 그대로(회고 5번 방식) 옮겨 적었다면
그 부분도 같이 갱신할 것. must_include에 "81"이나 "체어제로" 같은 옛 근거가
남아있으면 지울 것.

### 3) caveat(단서 문구)를 twin_lookup 하나의 특수 처리가 아니라
   "모든 tool 함수의 공통 반환 규칙"으로 승격시킬 것

회고 4번에서 LT 그룹의 "부모보다 32% 비쌈"이라는 문장을 LLM이 직접 부호를
계산하게 두지 않고, twin_lookup이 미리 문자열로 확정해서 반환하도록 고쳐서
버그를 잡았다. 회고 6번에서는 Q18(쌍둥이 그룹이 어떤 축만 공유하는지)이
똑같은 종류의 문제인데도 "트랩 하나에 맞춘 특수 규칙 추가는 지양한다"는
설계 원칙과 부딪혀서 보류해뒀다.

이 둘을 나눠서 생각한 게 원래 문제다. **사실 이 둘은 같은 문제의 두 사례다.**
"LLM이 raw 데이터를 보고 직접 문장을 조립하게 두면, 실행마다 부호를 틀리거나
조건을 빠뜨린다"가 공통 원인이다. 그러니 "Q18을 위한 특수 규칙"이 아니라
"비교나 부분일치가 들어간 tool 결과는 전부 완성된 문장을 미리 만들어서
반환한다"는 일반 규칙으로 이름을 바꾸면, 설계 원칙과 부딪히지 않는다.
LT 가격% 문장은 이미 이 일반 규칙의 사례 하나였을 뿐이다.

**구체적으로 twin_lookup이 반환하는 dict에 `caveat`라는 필드를 하나
추가한다.** 이 필드에는 아래 로직으로 완성된 한국어 문장을 미리 넣어둔다.

- group_id가 `LT2`나 `LT3`이면 → 기존처럼 "(추정: 실측 아닌 상속 규칙
  기반)"을 넣는다. 다만 LT2(체어원 LT)는 볼핏 1축만 실측이고 나머지
  4축은 추정이므로, 가능하면 "볼핏만 실측, 나머지 4축은 상속 추정"처럼
  어느 축이 실측인지까지 구체적으로 적어주는 게 더 낫다. LT3(선셋체어 LT)는
  5축 전부 추정이므로 "5축 전부 상속 규칙 기반"이라고 적는다.
- group_id가 진짜 쌍둥이(TWIN1·TWIN2·TWIN3)면 → 멤버들의 5축 값을 실제로
  비교해서, 전부 같으면 "5축 전부 동일", 일부만 같으면 "N개 축만 동일:
  [축 이름 나열]"처럼 자동으로 계산해서 문장을 만든다. 지금은 TWIN1·
  TWIN3이 5축 전부 동일이라 "전부 동일"이 나오겠지만, 이 로직 자체는
  일부만 같은 경우에도 그대로 쓸 수 있게 짜둘 것 — 그래야 나중에 택티컬·
  HDB 데이터가 들어와서 부분 일치 쌍둥이가 생겨도 코드를 또 안 고친다.

그리고 시스템 프롬프트에 한 줄을 추가한다: **"tool 결과에 caveat 필드가
있으면, 그 문장을 답변에 그대로 포함시켜라. 요약하거나 생략하지 마라."**
이러면 LLM이 문장을 새로 짓지 않고 이미 확정된 문장을 옮기기만 하면
되므로, 회고 6번에서 관찰된 run-to-run 누락(같은 질문인데 어떨 땐 caveat가
빠지는 것)이 줄어들 것으로 예상된다. 다만 이건 예상이라 실측이 필요하다
— 그래서 아래 4번이 필요하다.

### 4) eval_questions.csv에 문항 2개 추가됨 (20문항 → 22문항)

Q21은 LT2(체어원 LT) 관련, Q22는 LT3(선셋체어 LT) 관련이다. 지금까지
LT 그룹을 직접 묻는 문항이 하나도 없어서, "(추정: ...)" 문구가 실제
답변에 뜨는지 한 번도 시험된 적이 없었다. 3번 작업(caveat 일반화)이
끝나면 이 두 문항으로 먼저 확인할 것. main.py의 EVAL_CRITERIA에도
Q21·Q22 항목을 추가해야 `--eval` 모드가 22문항 전부를 채점한다.
종료 조건은 원래 "20문항 중 18개(90%)"였으니, 22문항 기준으로는
20개(90.9%, 반올림해서 20/22)를 새 기준으로 삼을 것을 권한다.

### 5) rules.md 열린 이슈 1번(그라운드시트 인치 매핑)은 폐기됐다

사용자가 직접 재검증한 결과, 공식몰은 인치가 아니라 One/Two/XL 같은
의자별 명칭 그대로 판매한다. v1 코드에 이 우려를 반영해서 "사이즈를
정확히 모른다"는 식으로 얼버무리는 응답 로직이나 프롬프트 문구가 있다면
찾아서 지울 것 — 이제는 명칭 그대로 안내해도 된다.
