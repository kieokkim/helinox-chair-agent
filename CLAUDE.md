## 모든 답변 공통

1. Be direct and concrete
   - 추상적 서술 대신 숫자와 파일명으로 말할 것
   - 예: "표본 부족" (X) → "48건 있는데 144건 필요, 3배 모자람" (O)

2. Show all intermediate states
   - 결론만 주지 말고 그 숫자가 나온 사이 계산을 전부 노출할 것
   - 예: "7.2년 필요" (X) → "81건이 4년치 → 연 20건 → 144÷20 = 7.2년" (O)

3. Explain like I'm 12
   - 어려운 용어는 지우지 말고, 옆에 쉬운 말을 괄호로 붙일 것
   - 예: "bootstrap CI가 0을 포함 (= 데이터를 만 번 다시 섞어봐도
     '수익이 0일 가능성'을 매번 못 지웠다는 뜻)"
   - 용어 자체를 바꾸면 내 문서와 대화가 따로 놀기 때문에 이름은 유지할 것

## 작업 프로토콜

모든 비자명한 작업은 다음 순서를 따른다. 단계를 건너뛰지 않는다.

1. research → `docs/research_{작업명}.md` 작성. 코드 작성 금지.
2. 사용자 검토 및 인라인 메모
3. plan → `docs/plan_{작업명}.md` 작성. 코드 작성 금지.
4. 사용자 메모 → 반영 → 반복 (사용자가 만족할 때까지)
5. 구현. 단계 완료 시 plan 문서에 완료 표시.

## research 문서 규칙

- 담는 것: 이번 작업이 호출하거나 의존할 지점의 **현재 실제 동작**
- 담지 않는 것: 프로젝트 개요, 진행 경과, 남은 계획
  (docs/ROADMAP.md, docs/NEXT_SESSION.md 와 중복)
- 모든 항목에 근거 위치를 `파일명:줄번호` 형식으로 명시
- 코드에서 직접 확인 못 한 항목은 `[미확인]` 표시. 추측으로 채우지 않는다.
- 목표 분량 20~40줄. 100줄을 넘으면 범위를 잘못 잡은 것이다.

## plan 문서 규칙

- 신규/수정 파일 경로, import할 기존 모듈, 실행 단계, 트레이드오프를 포함
- 기존 결정(DECISION_LOG의 D번호)이 걸리는 단계에는 해당 D번호를 명시
- 목표 분량 30~60줄

## 금지사항

- **호환 판정값 생성 금지.** ballfeet/shade_mm/groundsheet/rockingfoot/
  cupholder 값은 data/compat_table.csv에 있는 것만 사용한다.
  파일에 없으면 "데이터 없음"을 출력한다. 추론하지 않는다.
- **스펙 수치 생성 금지.** 무게·내하중·좌고·가격은 CSV에 없으면 비워둔다.
- 사용자 승인 전 코드 작성 금지.
- git push는 명시적 지시가 있을 때만.

## 방향이 틀렸을 때

패치로 고치지 않는다. `git reset` 또는 `git revert`로 되돌리고 범위를 좁혀
다시 시작한다.

## 작업 프로토콜
(기존 5단계 — 변경 없음)

## 구현 명령 시 표준 조건          ← 신규
구현 단계에서는 다음이 항상 적용된다:
- 작업/단계 완료 시 plan 문서에 완료로 표시
- 모든 단계가 끝날 때까지 멈추지 않는다
- 타입 힌트를 생략하거나 Any로 채우지 않는다
- 수정할 때마다 기존 테스트를 실행해 새 문제를 만들지 않는다

## 계획 문서는 자체 .md 파일로 작성한다   ← 신규
내장 plan mode를 쓰지 않는다. docs/ 아래 실제 파일로 남긴다.

## 자체 판단 금지 경계               ← 기존 금지사항에 1줄 추가
"계획이 충분하다"는 판단은 사용자만 한다.

## MVP 상한

### plan 문서 첫 3줄 고정
docs/plan_{작업명}.md 는 다음 3줄로 시작한다. 하나라도 없으면 구현 진입 금지.

  목표:      (사용자가 쓴다. 왜 만드는가. 1문장)
  종료 조건: 입력 N개 중 M개를 X 기준으로 통과하면 끝
  시작 시각: YYYY-MM-DD HH:MM  (구현 진입 시 date로 기록)

### 시간
- 총 8시간. 배분 research 1h / plan 1h / 구현 5h / 회고 1h
- 각 단계 진입 시 `date`를 실행해 경과를 계산하고 1줄 보고한다.
  형식: "경과 2h10m / 8h. research+plan 배분 2h 대비 +10m."
- 8h 도달 시 완성 여부 무관하게 중단.
  git tag mvp-stop-{작업명} → 회고 1장 → 종료.

### 목표 변경 제안
목표는 사용자가 정한다. research·plan에서 목표와 어긋나는 사실이 나오면 제안한다.

- 제안 시점은 2곳뿐: research 문서 마지막, plan 문서 마지막.
  구현 중에는 제안하지 않는다.
  예외 1개 — 종료 조건 자체가 측정 불가능해진 경우.
- 제안 형식 4줄 고정:
    관측:   (파일명:행 근거)
    영향:   목표 또는 종료 조건의 어느 부분이 성립하지 않는가
    선택지: A 목표 유지 + 범위 축소 / B 목표 수정 / C 중단
    재배분: 각 선택지의 남은 시간 배분을 숫자로
- 제안 후 멈춘다. 사용자 응답 전 진행 금지.
- 게이트당 1회. 같은 건으로 반복 제안하지 않는다.

### 판단 경계
- 한다:    관측 보고, 선택지 나열, 시간 재계산
- 안 한다: "이 정도면 충분하다" / "B가 낫다" / 선택지 중 추천